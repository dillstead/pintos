TOP_SRCDIR = ../..
DEFINES = -DUSERPROG -DFILESYS
SOURCES = $(THREADS_SRC) $(DEVICES_SRC) $(LIB_SRC) $(USERPROG_SRC) \
	  $(FILESYS_SRC)
INCLUDES = -I$(TOP_SRCDIR)/threads -I$(TOP_SRCDIR)/devices \
	   -I$(TOP_SRCDIR)/lib -I$(TOP_SRCDIR)/userprog \
	   -I$(TOP_SRCDIR)/filesys

include ../../Makefile.inc

